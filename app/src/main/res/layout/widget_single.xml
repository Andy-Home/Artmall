<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/widget_single_image"
            android:layout_width="fill_parent"
            android:layout_height="150dp"
            android:scaleType="centerCrop" />

        <cn.iwgang.countdownview.CountdownView
            android:background="#e01A1A1A"
            android:id="@+id/widget_single_time"
            android:layout_width="match_parent"
            android:layout_height="20dp"
            android:layout_alignBottom="@id/widget_single_image"
            app:isShowDay="true"
            app:isShowHour="true"
            app:isShowMillisecond="false"
            app:isShowMinute="true"
            app:isShowSecond="true"
            app:isTimeTextBold="true"
            app:suffix="剩："
            app:suffixDay="天"
            app:suffixGravity="bottom"
            app:suffixHour="时"
            app:suffixMinute="分"
            app:suffixSecond="秒"
            app:suffixTextColor="@color/white"
            app:suffixTextSize="12sp"
            app:timeTextColor="@color/white"
            app:timeTextSize="12sp" />
    </RelativeLayout>


    <TextView
        android:id="@+id/widget_single_title"
        android:layout_width="match_parent"
        android:layout_height="25dp"
        android:paddingTop="5dp"
        android:text="example" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="20dp"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/widget_single_price"
            android:layout_width="0dp"
            android:layout_height="20dp"
            android:layout_weight="1"
            android:text="example"
            android:textColor="#FF4500" />

        <com.count.andy.widget.HammerWidget
            android:id="@+id/widget_single_hammer"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="end" />

    </LinearLayout>

</LinearLayout>
